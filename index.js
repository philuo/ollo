/**
 * @file Vue3环境插件
 * @date 2022-07-10
 * @author Perfumere
 */

const { scroller } = require('./scroller');
const { longtouch } = require('./longtouch');

module.exports = {
    scroller,
    longtouch
};
